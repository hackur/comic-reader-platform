# Sample comic slot

Drop a `sample.cbz` (or `.cbt` / `.pdf`) into this directory if you want the app
to ship with a built-in demo comic.

The reader is local-first and will only load this file if the homepage explicitly
fetches it; nothing here is auto-imported. Keep individual files under the
Cloudflare Pages 25 MiB per-asset limit on the Free tier.
